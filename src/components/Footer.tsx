const Footer = () => (
  <footer className="px-6 py-10 border-t border-border text-center">
    <p className="font-mono text-xs text-muted-foreground">
      © {new Date().getFullYear()} · Built with genomic precision
    </p>
  </footer>
);

export default Footer;
